"""Stage 1.2: turning collected documents into feed cards.

The pipeline steps (normalize, dedup, prompts, schema, llm) live here; the entry
point that drives them is `src/services/processing_service.py`.
"""
